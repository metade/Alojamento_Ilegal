name: Análise trimestral

on:
  schedule:
    - cron: '0 7 1 1,4,7,10 *'
  workflow_dispatch:

concurrency:
  group: public-analysis
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: ruby/setup-ruby@866b91cc020931c510d42e43e498a7f07d335f60 # v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
      - name: Testes
        run: |
          bundle exec ruby -Itest test/al_ilegal_test.rb
          bundle exec ruby -Itest test/spatial_clusters_test.rb
          bundle exec ruby -Itest test/versioned_analysis_test.rb
      - name: Executar análise pública
        run: bundle exec ruby run_me.rb --mode public --reuse-existing
      - name: Preparar e validar outputs públicos
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add data/snapshots data/history
          ruby scripts/audit_publication.rb
      - name: Registar outputs públicos no histórico Git
        run: |
          git diff --cached --quiet || git commit -m "Atualiza análise trimestral"
          git push
      - name: Construir site público
        run: ruby scripts/build_site.rb
      - name: Gate do artefacto de publicação
        run: ruby scripts/audit_publication.rb --artifact site
      - name: Upload do site sanitizado
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      actions: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Publicar no GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4.0.5
